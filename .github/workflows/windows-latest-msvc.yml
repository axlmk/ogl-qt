name: windows-latest-msvc

on:
  push:
  pull_request:
    branches: [ "main" ]

jobs:
  windows-latest-build:
    uses: ./.github/workflows/windows-build.yml
    with:
      os: windows-latest
      configure: windows-msvc
      build: windows-msvc-b
      arch: win64_msvc2022_64