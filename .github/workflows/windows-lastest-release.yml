name: windows-latest-release-msvc

on:
  push:
  pull_request:
    branches: [ "main" ]

jobs:
  windows-latest-build:
    uses: ./.github/workflows/windows-build.yml
    with:
      os: windows-latest
      configure: windows-release
      preset: windows-release